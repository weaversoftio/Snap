apiVersion: v1
kind: Service
metadata:
  name: {{ include "snapui.fullname" . }}
  labels:
    {{- include "snapui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default "http" }}
      protocol: TCP
      name: http
  selector:
    {{- include "snapui.selectorLabels" . | nindent 4 }}
