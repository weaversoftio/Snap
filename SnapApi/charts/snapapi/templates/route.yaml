{{- if .Values.route.enabled -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "snapapi.fullname" . }}-route
  labels:
    {{- include "snapapi.labels" . | nindent 4 }}
spec:
  host: {{ .Values.route.host }}
  to:
    kind: Service
    name: {{ include "snapapi.fullname" . }}
    weight: 100
  port:
    targetPort: api
  wildcardPolicy: None
{{- end }}
