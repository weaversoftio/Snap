# Default values for snapapi.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: docker.io/weaversoftio/snapapi
  tag: "main"
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets:
  - name: dockerhub
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is false, "default" will be used
  # Set this to your existing service account name
  name: "snapapi-serviceaccount"

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

# Pod-level security context for OpenShift compatibility
# Sets fsGroup to ensure mounted volumes are writable by the container
# OpenShift runs containers with random UIDs but always adds them to group 0
# For more information: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/
podSecurityContext:
  fsGroup: 0

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: NodePort
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  apiPort: 8000
  webhookPort: 8443

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /docs
    port: api
  initialDelaySeconds: 40
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 3
readinessProbe:
  httpGet:
    path: /docs
    port: api
  initialDelaySeconds: 40
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 3

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Persistent storage configuration
persistence:
  enabled: true
  # Storage class to use for PVCs (leave empty for default storage class)
  storageClass: ""
  config:
    size: 1Gi
    # Use existing PVC instead of creating a new one
    existingClaim: ""
  checkpoints:
    size: 10Gi
    # Use existing PVC instead of creating a new one
    existingClaim: ""

# Additional volumes on the output Deployment definition.
# Note: When persistence.enabled is true, PVCs will be used instead
volumes: []

# Additional volumeMounts on the output Deployment definition.
volumeMounts: 
- name: config-volume
  mountPath: /app/config
- name: checkpoints-volume
  mountPath: /app/checkpoints

nodeSelector: {}

tolerations: []

affinity: {}

# Environment variables for snap configuration
env:
  # SnapApi configuration
  SNAP_ORIGINS: "http://localhost:8080,http://snapui.apps-crc.testing,*"
  SNAP_API_URL: "http://snapapi.apps-crc.testing"
  # SSL configuration (set to 'true' to enable SSL verification)
  KUBE_VERIFY_SSL: "false"
  # Cluster status check version requirements
  CRIO_VERSION_REQUIRED: "1.31.2"
  RUNC_VERSION_REQUIRED: "1.2.4"



# This block is for setting up OpenShift routes for external access
route:
  enabled: true
  host: snapapi.apps-crc.testing
  webhookHost: snapapi-webhook.apps-crc.testing
