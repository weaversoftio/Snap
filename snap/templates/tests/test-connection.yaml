apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snap.fullname" . }}-test-connection"
  labels:
    {{- include "snap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
