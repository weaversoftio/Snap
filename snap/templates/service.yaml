apiVersion: v1
kind: Service
metadata:
  name: {{ include "snap.fullname" . }}
  labels:
    {{- include "snap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snap.selectorLabels" . | nindent 4 }}
